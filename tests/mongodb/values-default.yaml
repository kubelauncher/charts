# MongoDB default test configuration (no auth)
auth:
  enabled: false

primary:
  resources:
    requests:
      memory: 512Mi
      cpu: 100m
    limits:
      memory: 1Gi
      cpu: 500m

persistence:
  enabled: false
