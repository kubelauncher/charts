# RabbitMQ Cluster test configuration
replicaCount: 3

auth:
  username: "admin"
  password: "adminpass123"
  erlangCookie: "secretcookie"

clustering:
  enabled: true

# RabbitMQ cluster needs more memory for Erlang runtime
resourcesPreset: "small"

persistence:
  enabled: false
