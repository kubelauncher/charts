# Redis Cluster test configuration (3 nodes, no replicas)
cluster:
  enabled: true
  nodes: 3
  replicas: 0
  resourcesPreset: "nano"
  persistence:
    enabled: false

auth:
  enabled: true
  password: "testpass123"
