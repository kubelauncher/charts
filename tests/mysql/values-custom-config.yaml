# MySQL with custom configuration
auth:
  rootPassword: "customroot123"
  database: "customdb"
  username: "customuser"
  password: "custompass123"

primary:
  configuration: |
    [mysqld]
    max_connections=150
    innodb_buffer_pool_size=128M
  resourcesPreset: "nano"
  persistence:
    enabled: false
  # Disable security contexts to test if it's a permission issue
  podSecurityContext:
    enabled: false
  containerSecurityContext:
    enabled: false
