{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "cassandra.fullname" . }}
  namespace: {{ include "cassandra.namespace" . }}
  labels: {{- include "cassandra.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{- include "cassandra.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        {{- if .Values.networkPolicy.allowExternal }}
        - podSelector: {}
        {{- else }}
        - podSelector:
            matchLabels: {{- include "cassandra.selectorLabels" . | nindent 14 }}
        {{- end }}
      ports:
        - protocol: TCP
          port: {{ .Values.containerPorts.cql }}
        - protocol: TCP
          port: {{ .Values.containerPorts.intraNode }}
        - protocol: TCP
          port: {{ .Values.containerPorts.tlsIntraNode }}
        {{- range .Values.networkPolicy.additionalRules }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
  egress:
    - {}
{{- end }}
