apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql.fullname" . }}
  namespace: {{ include "postgresql.namespace" . }}
  labels: {{- include "postgresql.labels" . | nindent 4 }}
    app.kubernetes.io/component: primary
  {{- with (merge (.Values.primary.service.annotations | default dict) (.Values.commonAnnotations | default dict)) }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.primary.service.type }}
  {{- if and (eq .Values.primary.service.type "ClusterIP") .Values.primary.service.clusterIP }}
  clusterIP: {{ .Values.primary.service.clusterIP }}
  {{- end }}
  ports:
    - name: tcp-postgresql
      port: {{ include "postgresql.servicePort" . }}
      targetPort: tcp-postgresql
      {{- if and (eq .Values.primary.service.type "NodePort") .Values.primary.service.nodePorts.postgresql }}
      nodePort: {{ .Values.primary.service.nodePorts.postgresql }}
      {{- end }}
    {{- if .Values.metrics.enabled }}
    - name: metrics
      port: {{ .Values.metrics.containerPorts.metrics }}
      targetPort: metrics
    {{- end }}
  selector: {{- include "postgresql.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: primary
