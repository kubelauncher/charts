apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql.fullname" . }}
  namespace: {{ include "mysql.namespace" . }}
  labels: {{- include "mysql.labels" . | nindent 4 }}
    app.kubernetes.io/component: primary
  {{- with (merge (.Values.primary.service.annotations | default dict) (.Values.commonAnnotations | default dict)) }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.primary.service.type }}
  {{- if and (eq .Values.primary.service.type "ClusterIP") .Values.primary.service.clusterIP }}
  clusterIP: {{ .Values.primary.service.clusterIP }}
  {{- end }}
  ports:
    - name: tcp-mysql
      port: {{ include "mysql.servicePort" . }}
      targetPort: tcp-mysql
      {{- if and (eq .Values.primary.service.type "NodePort") .Values.primary.service.nodePorts.mysql }}
      nodePort: {{ .Values.primary.service.nodePorts.mysql }}
      {{- end }}
  selector: {{- include "mysql.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: primary
